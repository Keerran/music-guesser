import Input from "./Input.svelte";
import Option from "./Option.svelte";
import Options from "./Options.svelte";
import SelectRoot from "./Select.svelte";

export const Select = Object.assign(SelectRoot, {Input, Options, Option})